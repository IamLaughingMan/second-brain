.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/graph.json
.obsidian/plugins/
.smart-connections/
.obsidian-git-data
.trash/
.DS_Store
node_modules/

# Claude history/: 原始對話 archive，含 prompt/response、可能夾 secret/PII
# Hub MOC + .base 保留追蹤；session 內容唔 push
Claude history/20*/

# ---- Binary / heavy assets：local-only，唔 sync 上 GitHub（決定 2026-06-30）----
# 目的：防 repo 爆大。bookmark snapshot 圖 + raw 源附件只留本機。
# ⚠️ 代價：呢啲圖唔上 GitHub = 冇 offsite 備份，靠本機 Time Machine/iCloud。
# 保留 .gitkeep → 空夾結構照入 git，fresh clone 有齊夾、淨係內容唔上。
raw/assets/*
!raw/assets/.gitkeep
raw/screenshots/*
!raw/screenshots/.gitkeep
raw/data/*
!raw/data/.gitkeep
Bookmarks/assets/*
!Bookmarks/assets/.gitkeep

